spring:
  application:
    name: g_74_shop
    #   вказуємо реквізити підключення до тестової бази h2
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: sa
    password:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml